language: node_js
node_js:
  - "10"
env:
  - TRAVIS_BUILD_DIR=./client/
dist: trusty
sudo: false
before_install:
  - yarn install
  - yarn global add @vue/cli-service-global
cache: yarn
script:
  - vue build
notifications:
  email:
    on_success: never
